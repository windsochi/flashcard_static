<%= @random_card.original_text %>

<%= simple_form_for "check#index", method: :get do |f| %>
  <%= f.input_field :search %>
  <%= f.submit 'Verify' %>
<% end %>

<p>
  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, class: name %>
  <% end %>
</p>
